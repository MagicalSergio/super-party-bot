services:
  app:
    build: .
    image: party-bot-test
    container_name: party-bot-server-test
    volumes:
      - "./src:/app/src"
      - "./data:/app/data"
    ports:
      - "8080:80"
    command: ["npm", "run", "dev"]
